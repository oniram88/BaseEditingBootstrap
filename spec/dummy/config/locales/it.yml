# Files in the config/locales directory are used for internationalization and
# are automatically loaded by Rails. If you want to use locales other than
# English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more about the API, please read the Rails Internationalization guide
# at https://guides.rubyonrails.org/i18n.html.
#
# Be aware that YAML interprets the following case-insensitive strings as
# booleans: `true`, `false`, `on`, `off`, `yes`, `no`. Therefore, these strings
# must be quoted to be interpreted as strings. For example:
#
#     en:
#       "yes": yup
#       enabled: "ON"

it:
  activerecord:
    attributes:
      post/category:
        other: "Altro"
        project: "Progetti"
        project_html: "Progetti<br>" # Version implementata per simulare il test
      post:
        priority/help_text: "Priority Helper text"
        priority/unit: "priority unit"
        description: Desc
        description/help_text_html: "Helper con <b>html</b>"
        title: Titolo
      user:
        enabled/help_text: Abilitato
    successful:
      messages:
        user:
          destroyed: Congratulazioni, hai cancellato l'utente.
    unsuccessful:
      messages:
        user:
          updated: Qualcosa è andata storta e non siamo riusciti a salvare il l'utente.
  errors:
    attributes:
      base:
        indestructible: "Indistruttibile"
  base_editing:
    search_result_buttons:
      edit: Modifica
  posts:
    index:
      base_editing:
        search_result_buttons:
          edit: MOD Post
          show: Show Post Inherited
  customer/posts:
    index:
      base_editing:
        search_result_buttons:
          edit: Customer Mod Post